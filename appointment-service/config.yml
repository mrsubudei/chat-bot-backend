app:
  name: 'chat-bot-backend'
  version: '1.0.0'

grpc:
  host: '0.0.0.0'
  port: '8087'
  maxConnectionIdle: 5 # Minutes
  timeout: 15 # Seconds
  maxConnectionAge: 5 # Minutes

postgres:
  pool_max: 2
  url: "postgres://user:pass@pgapoi:5433/apoi"

logger:
  log_level: 'debug'
  rollbar_env: 'chat-bot-backend'